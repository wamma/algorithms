#include <bits/stdc++.h>
using namespace std;

static const int dx[4] = {-1, 1, 0, 0};
static const int dy[4] = {0, 0, -1, 1};

int n;
vector<vector<int>> mapGrid;
vector<vector<bool>> visited;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    cin >> n;
    mapGrid.assign(n, vector<int>(n, 0));
	visited.
}